# fff - Codebase Index
# Generated: 2026-05-31T02:58:06.843Z via deepseek-chat

project:
  name: fff
  version: 1.0.0
  description: "fff (fucking fucking fast) — a fast terminal AI coding assistant"
  language: TypeScript
  runtime: Bun

files:
  - path: codebase-index.yaml
    description: "Provides a structured index of the project's metadata, entry points, and source files for the AI assistant."
  - path: install.sh
    description: "Automates the installation of the fff tool by downloading and setting up dependencies like Bun."
  - path: package.json
    description: "Defines the fff project's metadata, dependencies, scripts, and entry point for Node.js/Bun."
  - path: PI_PORT_TODO.md
    description: "Documents the porting progress of pi's prompt box and scrolling features into fff, listing implemented and remaining work."
  - path: README.md
    description: "Describes fff as a terminal AI coding assistant, its features, installation steps, and requirements."
  - path: src/agent-runner.ts
    description: "Orchestrates the agent loop by executing LLM calls, tool invocations, and managing conversation state."
  - path: src/app.tsx
    description: "Serves as the main React component for the TUI, managing state for messages, streaming, and input."
  - path: src/clipboard.ts
    description: "Provides a cross-platform function to copy text to the system clipboard."
  - path: src/config.ts
    description: "Exports color constants and system prompt configuration for the fff terminal UI."
  - path: src/conversation.ts
    description: "Provides utilities for pruning conversation messages and estimating token counts."
  - path: src/index.tsx
    description: "Entry point that dispatches between the non-interactive indexer subcommand and the interactive TUI."
  - path: src/indexer.ts
    description: "Walks the project source files and generates a codebase-index.yaml with AI-generated summaries."
  - path: src/input-editor.ts
    description: "Implements grapheme-aware text editing operations like insert, backspace, and word navigation."
  - path: src/input-panel.tsx
    description: "Renders the user input area with visual line wrapping and cursor positioning."
  - path: src/kill-ring.ts
    description: "Implements an Emacs-style kill ring for managing killed text entries with yank support."
  - path: src/llm.ts
    description: "Configures the OpenAI client and provides functions for LLM calls and tool description generation."
  - path: src/logger.ts
    description: "Provides a lightweight append-only logger that writes per-session logs to a file."
  - path: src/message-format.ts
    description: "Provides utilities for wrapping text and parsing code blocks and tool invocations from messages."
  - path: src/message-line.tsx
    description: "Renders a single message line with syntax highlighting for tool calls and code blocks."
  - path: src/message-viewport.tsx
    description: "Renders the scrollable viewport of conversation messages with streaming preview support."
  - path: src/paste.ts
    description: "Handles bracketed-paste markers from terminal input, normalizing large pastes into compact markers for editor readability."
  - path: src/pi-prompt-utils.ts
    description: "Provides utilities for wrapping input text into visual lines based on terminal width, supporting cursor navigation."
  - path: src/process-registry.ts
    description: "Central registry for tracking spawned child processes to ensure they are killed on agent exit."
  - path: src/session-store.ts
    description: "Manages persistent session storage to JSON files for conversation history and resume functionality."
  - path: src/text-segmentation.ts
    description: "Provides grapheme and word segmentation utilities using Intl.Segmenter for text navigation."
  - path: src/tools-registry.ts
    description: "Registers and types tool definitions for file operations and command execution."
  - path: src/tools.ts
    description: "Implements file and command execution tools with path resolution, timeout, and output limits."
  - path: src/types.ts
    description: "Defines TypeScript types for application state, segments, history, and agent refs."
  - path: src/undo-stack.ts
    description: "Implements a generic undo stack with deep-cloned state snapshots."
  - path: src/use-alternate-screen.ts
    description: "No-op hook for alternate screen lifecycle, now managed by the entry point."
  - path: src/use-app-input.ts
    description: "Handles keyboard input for the application, integrating editor state, history, and paste handling."
  - path: src/viewport.ts
    description: "Builds a viewport model for rendering messages with scrolling and streaming support."
  - path: src/word-navigation.ts
    description: "Provides word-boundary navigation functions for cursor movement in text."
  - path: tsconfig.json
    description: "TypeScript configuration for ESNext target, bundler module resolution, and strict mode."
